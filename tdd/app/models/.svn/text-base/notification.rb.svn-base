class Notification < ActiveRecord::Base
  # stores any actual notifications for a group
  
  

  belongs_to :notification_type  # what type of notification is it
  belongs_to :group # wht group does it belong to

  
end
